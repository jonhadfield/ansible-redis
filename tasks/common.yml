---
- name: create base installation directory
  file: path={{ redis_base_install_dir }} mode=0755 state=directory
- name: create logs directory
  file: path={{ redis_base_logs_dir }} mode=0755 state=directory
- name: create base data directory
  file: path={{ redis_base_data_dir }} mode=0755 state=directory
